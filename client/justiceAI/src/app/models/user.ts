import { Base } from './base';

export class User extends Base {
  email: string;
  userName: string;
  role: number;
  roleName: string;
  fullName: string;
  phoneNumber: string;
  firstName: string;
  lastName: string;
}
